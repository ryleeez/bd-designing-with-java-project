# -*-perl-*-

package.ATACurriculumSustainabilityShipmentServiceIntrospectionTests = {
    interfaces = (C2020July);
    deploy = {
        generic = true;
    };

    dependencies = {
        C2020July = {
            ATACurriculumTestInfrastructure = 1.0;
            ATACurriculumTctIntrospection = 1.0;

            BONESLambdaJavaRouter = 2.3;

            CoralModel = 1.1;

            GoogleGuava = 23.x;

            JakartaCommons-IO = 2.5;
            JUnit5 = 5.x;

            # Unconventional but intentional: we want Mock annotations to be active in runtime,
            # which is when Introspection Tests inspect unit tests that participants have written
            # in the base package.
            Mockito = 2.x;

            Reflections = 0.9.x;
        };
    };

    tst-dependencies = {
        C2020July = {
        };
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = happytrails;

    build-tools = {
        C2020July = {
            ATACheckstyle = 1.1;
            ATACurriculumTctIntrospectionModel = 1.0;
            CoralGenerator = 1.2;
            JDK8 = 1.0;
            HappierTrails = 3.4;
        };
    };

    resolves-conflict-dependencies = {
        C2020July = {
            GoogleGuava = 23.x;
        };
    };

    # For more information on how to declare targets for HappyTrails packages
    # see: https://w.amazon.com/?BrazilBuildSystem/HappyTrails/GettingStarted
    targets = {
        ATACurriculumSustainabilityShipmentServiceIntrospectionTests-C2020July = { type = javalibrary; };
    };
};
